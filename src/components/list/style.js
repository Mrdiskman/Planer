import { styled } from "styled-components";

export const ListContainer = styled.div`
    display: flex;
    flex-direction: column;
    .animationContainer{
        display: flex;
        flex-direction: column;
        gap: 20px;
    }
`

